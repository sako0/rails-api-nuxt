// import createPersistedState from 'vuex-persistedstate'
//
// export default ({ store }) => {
//   window.onNuxtReady(() => {
//     createPersistedState({
//       key: 'user',
//       // Session Storageを利用したい場合は下記を追加（ブラウザを閉じると値はなくなる）
//       // storage: window.sessionStorage,
//     })(store)
//   })
// }
